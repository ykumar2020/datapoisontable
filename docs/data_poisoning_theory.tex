\documentclass[conference]{IEEEtran}

\usepackage{amsmath}
\usepackage{amssymb}
\usepackage{array}
\usepackage{booktabs}
\usepackage{cite}
\usepackage{graphicx}
\usepackage{hyperref}
\usepackage{url}
\usepackage{xcolor}

\graphicspath{{../figures/}{figures/}}

\title{Periodic Table of Data Poisoning: A Risk-Based Framework for AI Security}

\author{
\IEEEauthorblockN{Armando Luna}
\IEEEauthorblockA{
Kean University\\
Email: lunaarm@kean.edu}
\and
\IEEEauthorblockN{Mentor: Yulia Kumar}
\IEEEauthorblockA{
Kean University}
}

\begin{document}
\maketitle

\begin{abstract}
Data poisoning attacks manipulate the data used to train, fine-tune, align, retrieve for, or otherwise condition machine learning systems. This paper presents a risk-based framework for organizing poisoning and related adversarial methods into a ``Periodic Table of Data Poisoning.'' The framework separates training-time poisoning from test-time evasion, groups techniques by mechanism, and scores operational danger using impact, stealth, scalability, and cost-efficiency. A real MNIST handwritten-digit experiment supports the framework with executable comparisons across label poisoning, subpopulation poisoning, backdoor poisoning, feature-collision poisoning, and several related evasion attacks. The results show that backdoors and subgroup-targeted poisoning can preserve high aggregate clean accuracy while creating severe targeted failure modes, supporting the need for risk-aware evaluation beyond ordinary accuracy.
\end{abstract}

\begin{IEEEkeywords}
data poisoning, backdoor attacks, adversarial machine learning, MNIST, AI security, risk assessment
\end{IEEEkeywords}

\section{Introduction}

Machine learning systems are increasingly trained from data that is collected, labeled, filtered, retrieved, or reused through complex pipelines. These pipelines create an attack surface: an adversary may influence the data before training, fine-tuning, retrieval, preference optimization, or deployment-time adaptation. Data poisoning attacks exploit this surface by modifying a small part of the data so that the learned model behaves incorrectly.

The original poster project organized adversarial techniques into a visual ``Periodic Table.'' This paper strengthens that idea in three ways. First, it separates data poisoning from test-time evasion. Many well-known methods, including FGSM, PGD, JSMA, ZOO, SparseFool, HopSkipJump, and Adversarial Patch, modify inputs at inference time and should not be labeled as poisoning unless their outputs are inserted into the training set. Second, it introduces a risk model based on operational danger rather than mathematical elegance alone. Third, it provides real executable MNIST experiments so the table is supported by measured results instead of only descriptions.

The contributions are:
\begin{itemize}
    \item a mechanism-based taxonomy for poisoning and related adversarial methods;
    \item a risk score based on impact, stealth, scalability, and cost-efficiency;
    \item real MNIST implementations for multiple poisoning, backdoor, and evasion methods;
    \item an implementation audit that distinguishes full implementations from simplified or theory-only entries.
\end{itemize}

\section{Related Work}

Classical data poisoning was studied for support vector machines and other learners by Biggio et al.~\cite{biggio2012} and Mei and Zhu~\cite{mei2015}, who formalized poisoning as an optimization problem over training data. Backdoor poisoning became a central model-supply-chain threat with BadNets~\cite{badnets} and targeted backdoor attacks~\cite{targetedbackdoor}. Clean-label poisoning methods such as Poison Frogs~\cite{poisonfrogs} increased stealth by keeping human-visible labels correct. Gradient-matching methods such as Witches' Brew~\cite{witchesbrew} further improved poisoning efficiency by aligning poison gradients with a target objective.

Modern systems introduce additional poisoning surfaces. Web-scale dataset poisoning demonstrates that mutable public data can compromise large training collections~\cite{webscale}. RAG poisoning attacks retrieval corpora rather than model weights~\cite{poisonedrag}. Generative-model poisoning, including Nightshade~\cite{nightshade}, targets text-to-image training data. Backdoor variants such as clean-label backdoors~\cite{cleanlabelbackdoor} and WaNet~\cite{wanet} improve stealth by hiding or warping triggers.

The adversarial-example literature provides related but distinct test-time methods. JSMA uses saliency maps over the model Jacobian~\cite{jsma}; EAD uses an elastic-net optimization objective~\cite{ead}; ZOO estimates gradients with finite differences in black-box settings~\cite{zoo}; HopSkipJump is a query-efficient decision-based attack~\cite{hopskipjump}; Adversarial Patch learns a reusable localized patch~\cite{adversarialpatch}; and SparseFool searches for sparse decision-boundary crossings~\cite{sparsefool}. These methods are useful comparison baselines, but they are not training-time data poisoning by default.

\section{Methodology}

\subsection{Poisoning Definition}

Let a supervised training set be
\begin{equation}
    D = \{(x_i, y_i)\}_{i=1}^{n},
\end{equation}
where \(x_i\) is an input and \(y_i\) is a label. The learner trains parameters \(\theta\) by solving
\begin{equation}
    \theta^\star(D) =
    \arg\min_{\theta}
    \frac{1}{n}\sum_{i=1}^{n}\ell(f_\theta(x_i), y_i)
    + \lambda \Omega(\theta).
\end{equation}
In data poisoning, the attacker modifies the training data:
\begin{equation}
    D' = D_{\mathrm{clean}} \cup D_{\mathrm{poison}},
\end{equation}
so that the resulting model \(\theta^\star(D')\) differs from the clean model \(\theta^\star(D_{\mathrm{clean}})\) in a way that benefits the attacker.

A general poisoning objective can be written as a bilevel problem:
\begin{equation}
\begin{aligned}
    \max_{D_{\mathrm{poison}} \in \mathcal{C}} \quad
        & \mathcal{A}(\theta^\star(D_{\mathrm{clean}} \cup D_{\mathrm{poison}}))
        - c(D_{\mathrm{poison}}) \\
    \mathrm{s.t.} \quad
        & \theta^\star(D_{\mathrm{clean}} \cup D_{\mathrm{poison}})
        =
        \arg\min_{\theta}
        \mathcal{L}(\theta; D_{\mathrm{clean}} \cup D_{\mathrm{poison}}).
\end{aligned}
\end{equation}
Here, \(\mathcal{A}\) is the adversary's objective, \(c\) is a cost or detectability penalty, and \(\mathcal{C}\) captures the attacker's access constraints.

\subsection{Mechanism Categories}

The framework uses mechanism categories as table columns:
\begin{itemize}
    \item G: gradient-based methods;
    \item O: optimization-based methods;
    \item M: minimal or structural changes;
    \item U: universal or transferable methods;
    \item B: backdoor or trigger-based methods;
    \item S: supply-chain and web-scale methods;
    \item R: retrieval or RAG corpus poisoning;
    \item P: preference, reward, or alignment-data poisoning.
\end{itemize}

\subsection{Risk Model}

Operational danger is scored as
\begin{equation}
    \mathrm{DangerScore}=I \times S \times G \times C,
\end{equation}
where \(I\) is impact, \(S\) is stealth, \(G\) is scalability, and \(C\) is cost-efficiency. Each factor is rated from 1 to 5, with 5 indicating greater danger for defenders. Cost-efficiency is high when the attack is cheaper or easier for the adversary.

\begin{table}[t]
\centering
\caption{Risk levels used in the periodic table.}
\label{tab:risk}
\begin{tabular}{lll}
\toprule
Level & Score & Meaning \\
\midrule
R1 & 400--625 & Extreme operational danger \\
R2 & 200--399 & High real-world concern \\
R3 & 100--199 & Moderate or constrained risk \\
R4 & 1--99 & Low or mainly illustrative risk \\
\bottomrule
\end{tabular}
\end{table}

\section{Attack Taxonomy}

Table~\ref{tab:taxonomy} consolidates the techniques suggested during project development. Duplicate labels are normalized into families. The most important correction is stage: poisoning modifies training, fine-tuning, retrieval, feedback, or model-supply-chain data, while evasion modifies test-time inputs.

\begin{table*}[t]
\centering
\caption{Consolidated attack taxonomy for the periodic table.}
\label{tab:taxonomy}
\small
\begin{tabular}{p{0.19\textwidth}p{0.10\textwidth}p{0.09\textwidth}p{0.54\textwidth}}
\toprule
Technique family & Stage & Mechanism & Summary \\
\midrule
Label flipping & Poisoning & M & Changes training labels only. It is cheap and scalable, but easier to detect when classes are visually separable. \\
Targeted label flipping & Poisoning & M & Changes labels for a chosen source class or source subpopulation to steer a specific confusion. \\
Subpopulation poisoning & Poisoning & M & Attacks a naturally occurring slice while aggregate accuracy remains high. \\
Poison Frogs / feature collision & Poisoning & O & Optimizes clean-label samples so their feature representation collides with a target instance. \\
Bullseye / convex polytope & Poisoning & O & Uses several clean-label poisons to surround a target in feature space. \\
Witches' Brew / gradient matching & Poisoning & G/O & Crafts poisons whose training gradients align with a target objective. \\
Influence, Hessian, curriculum poisoning & Poisoning & O & Uses high-influence samples, second-order curvature, or training order to maximize downstream damage. \\
KNN / geometric poisoning & Poisoning & M & Inserts or relabels geometrically strategic samples to alter local neighborhoods or margins. \\
BadNets and patch backdoors & Poisoning & B & Adds triggered training examples so clean accuracy remains high but triggered inputs map to the attacker target. \\
Clean-label, hidden, WaNet, Sleeper Agent & Poisoning & B & Backdoors designed to avoid label noise, visible trigger artifacts, clustering defenses, fine-tuning, or pruning. \\
Web-scale and supply-chain poisoning & Poisoning & S/B & Attacks dataset collection or model reuse pipelines so one compromised artifact affects many downstream models. \\
RAG corpus poisoning & Poisoning & R & Inserts or optimizes retrieval documents so a RAG system grounds answers in poisoned evidence. \\
Nightshade and diffusion Trojans & Poisoning & G/B & Targets generative models through poisoned images or diffusion training paths. \\
FGSM, PGD, JSMA, SparseFool, EAD, ZOO, HopSkipJump & Evasion & G/O/M & Modify test inputs against an already trained model. \\
Adversarial Patch, UAP, EOT, surrogate transfer & Evasion & U & Universal or transferable test-time attacks reused across many inputs or transformations. \\
\bottomrule
\end{tabular}
\end{table*}

\section{Experimental Design}

\subsection{Dataset and Model}

The experiments use MNIST, a real handwritten-digit dataset with 60,000 training images and 10,000 test images~\cite{mnist}. Each input is a \(28 \times 28\) grayscale image. The code trains scikit-learn stochastic-gradient logistic classifiers, giving a transparent baseline whose gradients and decision boundaries are easy to inspect.

\subsection{Poisoning Implementations}

The comparison suite implements six training-time poisoning or backdoor conditions:
\begin{itemize}
    \item random label flipping;
    \item targeted source-to-target label flipping from digit 7 to digit 1;
    \item subpopulation label poisoning over a structurally selected digit-7 subgroup;
    \item BadNets-style visible patch backdoor;
    \item clean-label patch backdoor over target-class training images;
    \item prototype feature-collision poisoning.
\end{itemize}

For a backdoor trigger \(T\), the implementation uses a visible lower-right square:
\begin{equation}
    T(x) = (1-m)\odot x + m\odot v,
\end{equation}
where \(m\) is a binary mask and \(v\) is the trigger intensity. The backdoor metric is trigger attack success rate:
\begin{equation}
    \mathrm{ASR} =
    \frac{\sum_{(x,y):y\neq t} \mathbf{1}[\arg\max_k f_\theta(T(x))_k=t]}
    {\sum_{(x,y)} \mathbf{1}[y\neq t]}.
\end{equation}

For subpopulation poisoning, a structural score selects a slice of digit 7:
\begin{equation}
    q(x)=\mathrm{mean}(x_{\mathrm{upper\ right}})-\mathrm{mean}(x_{\mathrm{lower\ left}}).
\end{equation}
Only high-\(q(x)\) source examples are relabeled to the target. The metric is source-subgroup confusion to digit 1.

\subsection{Evasion Implementations}

The suite also implements related test-time attacks against the clean baseline: FGSM, PGD, EAD, JSMA, SparseFool-style sparse boundary crossing, Adversarial Patch, ZOO finite differences, and HopSkipJump-style boundary search. These are included because the original poster used several evasion methods, but the paper labels them as evasion rather than poisoning.

For evasion, untargeted success is
\begin{equation}
    \mathrm{USR} =
    \frac{1}{m}\sum_{i=1}^{m}
    \mathbf{1}[\arg\max_k f_\theta(x_i')_k \neq y_i],
\end{equation}
and targeted success toward class \(t\) is
\begin{equation}
    \mathrm{TSR} =
    \frac{1}{m}\sum_{i=1}^{m}
    \mathbf{1}[\arg\max_k f_\theta(x_i')_k = t].
\end{equation}

\section{Results}

The expanded comparison was run on the full MNIST split. The target digit was \(t=1\), and the source digit was \(s=7\). Table~\ref{tab:results} reports clean accuracy and the attack-specific success or damage metric.

\begin{table*}[t]
\centering
\caption{MNIST attack comparison. Poisoning methods retrain on modified training data; evasion methods attack the clean baseline.}
\label{tab:results}
\small
\begin{tabular}{p{0.20\textwidth}p{0.10\textwidth}p{0.08\textwidth}p{0.22\textwidth}p{0.08\textwidth}p{0.24\textwidth}}
\toprule
Method & Stage & Clean acc. & Metric & Value & Notes \\
\midrule
Clean baseline & Reference & 0.9173 & \(7 \rightarrow 1\) confusion & 0.0117 & Full MNIST train/test split. \\
Random label flip & Poisoning & 0.8914 & Clean accuracy drop & 0.0259 & 6000 labels changed. \\
Targeted label flip & Poisoning & 0.9074 & \(7 \rightarrow 1\) confusion & 0.0525 & 2193 digit-7 labels changed. \\
Subpopulation label poison & Poisoning & 0.8972 & Subgroup-to-1 confusion & 0.4434 & 1504 source-subgroup labels changed. \\
BadNets patch backdoor & Poisoning & 0.9115 & Trigger ASR to 1 & 0.9988 & 3000 triggered non-target copies. \\
Clean-label patch backdoor & Poisoning & 0.9157 & Trigger ASR to 1 & 0.3717 & 3000 correctly labeled target images patched. \\
Prototype feature collision & Poisoning & 0.9174 & \(1 \rightarrow 7\) confusion & 0.0018 & Clean-label prototype blend. \\
FGSM & Evasion & 0.9173 & Untargeted misclassification & 1.0000 & Single-step gradient sign. \\
PGD & Evasion & 0.9173 & Untargeted misclassification & 1.0000 & Projected gradient ascent. \\
Elastic Net EAD & Evasion & 0.9173 & Targeted ASR to 1 & 0.9890 & L1/L2-style targeted update. \\
JSMA saliency & Evasion & 0.9173 & Targeted ASR to 1 & 0.6433 & Sparse saliency pixels. \\
SparseFool-style boundary & Evasion & 0.9173 & Untargeted misclassification & 1.0000 & Sparse linear-boundary crossing. \\
Adversarial patch & Evasion & 0.9173 & Untargeted misclassification & 0.8290 & Universal fixed-position patch. \\
ZOO finite difference & Evasion & 0.9173 & Targeted ASR to 1 & 0.0250 & 93,340 black-box queries. \\
HopSkipJump-style boundary & Evasion & 0.9173 & Targeted ASR to 1 & 0.9800 & Decision-only boundary search. \\
\bottomrule
\end{tabular}
\end{table*}

\begin{figure*}[t]
\centering
\includegraphics[width=0.95\textwidth]{mnist_attack_comparison_bars.png}
\caption{At-a-glance comparison generated by \texttt{scripts/mnist\_attack\_comparison.py}. Blue bars show clean accuracy; orange bars show the attack success or damage metric.}
\label{fig:bars}
\end{figure*}

The BadNets-style patch backdoor is the clearest poisoning demonstration: clean accuracy remains close to the baseline, while trigger ASR reaches 0.9988. Subpopulation poisoning is also important because aggregate clean accuracy remains close to 0.90, but the selected subgroup is heavily biased toward the attacker target. This supports the main framework claim: real risk is driven not only by global accuracy degradation, but by stealthy and targeted failure modes.

\section{Implementation Audit}

The requested seven-method list includes several methods that are fully implemented, partially implemented, or currently theory-only in this repository. Table~\ref{tab:audit} makes these claims explicit.

\begin{table*}[t]
\centering
\caption{Audit of requested seven methods against current executable code.}
\label{tab:audit}
\small
\begin{tabular}{p{0.22\textwidth}p{0.08\textwidth}p{0.14\textwidth}p{0.48\textwidth}}
\toprule
Requested method & Category & Status & Verification \\
\midrule
Poison Frogs / clean-label poisoning & O, R1 & Partial & Catalog and theory included. Code has \texttt{Prototype feature collision}, but not full constrained deep-feature Poison Frogs optimization. \\
Sleeper Agent & B, R1 & Theory only & Discussed in catalog and taxonomy. Current clean-label patch backdoor is not Sleeper Agent and does not test fine-tuning persistence. \\
Adversarial Patch & U, R2 & Partial & Implemented as universal fixed-position MNIST patch. Full EOT over scale, rotation, lighting, and placement is not yet implemented. \\
Witches' Brew / gradient matching & G, R2 & Theory only & Discussed and cited. No executable gradient-matching poisoning implementation is present. \\
JSMA & M, R3 & Full for current model & Implemented as sparse saliency attack. For the linear MNIST classifier, the Jacobian is the class-weight matrix. \\
ZOO & O, R3 & Partial & Finite-difference black-box attack with query counting is implemented, but not full Adam/ADMM ZOO. \\
SparseFool & M, R4 & Full for current model & Since the classifier is linear, SparseFool's local linearization reduces to sparse linear-boundary crossing, implemented directly. \\
\bottomrule
\end{tabular}
\end{table*}

\section{Discussion}

The comparison reveals three lessons. First, backdoors can preserve normal clean accuracy while creating a hidden targeted failure. This makes them more operationally dangerous than simple availability attacks that visibly degrade validation accuracy. Second, subgroup attacks show why aggregate metrics are insufficient; a model can look acceptable overall while failing badly on a selected slice. Third, evasion methods are useful for comparison, but their stage must be labeled correctly. A test-time adversarial example is not data poisoning unless it is used to contaminate training or adaptation data.

The current MNIST implementation is intentionally transparent and reproducible. It does not claim to reproduce every paper-grade method. The audit table is included to prevent overclaiming and to guide future implementation work.

\section{Safety and Reproducibility}

The experiments are local, use a public benchmark dataset, and do not target deployed systems, real users, private data, or production services. The repository excludes the downloaded MNIST data from version control; it can be re-downloaded through \texttt{torchvision}. The main commands are:
\begin{itemize}
    \item \texttt{python scripts/mnist\_poisoning\_experiment.py --download}
    \item \texttt{python scripts/mnist\_attack\_comparison.py --download}
\end{itemize}

\section{Conclusion}

This paper presents a risk-based periodic-table framework for data poisoning and related adversarial ML methods. The framework improves the poster by distinguishing poisoning from evasion, assigning operational risk levels, and grounding the discussion in real MNIST experiments. The results show that backdoor and subpopulation attacks can be especially dangerous because they preserve high aggregate accuracy while creating targeted failures. Future work should implement full Poison Frogs, Witches' Brew, Sleeper Agent, and EOT-based patch attacks with deep MNIST or CIFAR models, then compare their risk scores under the same measurement protocol.

\begin{thebibliography}{99}

\bibitem{mnist}
Y. LeCun, L. Bottou, Y. Bengio, and P. Haffner, ``Gradient-based learning applied to document recognition,'' \emph{Proceedings of the IEEE}, 1998.

\bibitem{biggio2012}
B. Biggio, B. Nelson, and P. Laskov, ``Poisoning attacks against support vector machines,'' ICML, 2012.

\bibitem{mei2015}
S. Mei and X. Zhu, ``Using machine teaching to identify optimal training-set attacks on machine learners,'' AAAI, 2015.

\bibitem{badnets}
T. Gu, B. Dolan-Gavitt, and S. Garg, ``BadNets: Identifying vulnerabilities in the machine learning model supply chain,'' arXiv:1708.06733, 2017.

\bibitem{targetedbackdoor}
X. Chen, C. Liu, B. Li, K. Lu, and D. Song, ``Targeted backdoor attacks on deep learning systems using data poisoning,'' arXiv:1712.05526, 2017.

\bibitem{poisonfrogs}
A. Shafahi et al., ``Poison Frogs! Targeted clean-label poisoning attacks on neural networks,'' NeurIPS, 2018.

\bibitem{witchesbrew}
J. Geiping et al., ``Witches' Brew: Industrial scale data poisoning via gradient matching,'' arXiv:2009.02276, 2020.

\bibitem{webscale}
N. Carlini et al., ``Poisoning web-scale training datasets is practical,'' IEEE Symposium on Security and Privacy, 2024.

\bibitem{poisonedrag}
W. Zou, R. Geng, B. Wang, and J. Jia, ``PoisonedRAG: Knowledge corruption attacks to retrieval-augmented generation of large language models,'' arXiv:2402.07867, 2024.

\bibitem{nightshade}
S. Shan et al., ``Nightshade: Prompt-specific poisoning attacks on text-to-image generative models,'' arXiv:2310.13828, 2023.

\bibitem{jsma}
N. Papernot, P. McDaniel, S. Jha, M. Fredrikson, Z. B. Celik, and A. Swami, ``The limitations of deep learning in adversarial settings,'' IEEE European Symposium on Security and Privacy, 2016.

\bibitem{ead}
P.-Y. Chen, Y. Sharma, H. Zhang, J. Yi, and C.-J. Hsieh, ``EAD: Elastic-net attacks to deep neural networks via adversarial examples,'' AAAI, 2018.

\bibitem{zoo}
P.-Y. Chen, H. Zhang, Y. Sharma, J. Yi, and C.-J. Hsieh, ``ZOO: Zeroth order optimization based black-box attacks to deep neural networks without training substitute models,'' ACM Workshop on Artificial Intelligence and Security, 2017.

\bibitem{hopskipjump}
J. Chen, M. I. Jordan, and M. J. Wainwright, ``HopSkipJumpAttack: A query-efficient decision-based attack,'' IEEE Symposium on Security and Privacy, 2020.

\bibitem{adversarialpatch}
T. B. Brown, D. Mane, A. Roy, M. Abadi, and J. Gilmer, ``Adversarial patch,'' arXiv:1712.09665, 2017.

\bibitem{sparsefool}
A. Modas, S.-M. Moosavi-Dezfooli, and P. Frossard, ``SparseFool: A few pixels make a big difference,'' CVPR, 2019.

\bibitem{cleanlabelbackdoor}
A. Turner, D. Tsipras, and A. Madry, ``Clean-label backdoor attacks,'' 2019.

\bibitem{wanet}
A. Nguyen and A. Tran, ``WaNet: Imperceptible warping-based backdoor attack,'' ICLR, 2021.

\end{thebibliography}

\end{document}
